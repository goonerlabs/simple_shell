#include "shell.h"

/**
 * main - main function to run simple_shell project
 *
 * Return: onSuccess(0), else (-1) 
*/

int main(int __attribute__((unused))argc, char **argv)
{
	shellStart(argv);

	return (0);
}
